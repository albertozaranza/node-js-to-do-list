# NodeJS to-do list
A simple to-do list made with NodeJS using Express with the following routes:

- `POST` /projects

- `GET` /projects

- `PUT` /projects/:id

- `DELETE` /projects/:id

- `POST` /projects/:id/tasks
